connector.name=hive
hive.metastore.uri=thrift://hive-metastore:9083 # You might need a Hive Metastore service for this to work fully.
s3.endpoint=http://minio:9000
s3.aws-access-key=${MINIO_ROOT_USER}
s3.aws-secret-key=${MINIO_ROOT_PASSWORD}